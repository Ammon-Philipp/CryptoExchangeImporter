# Overview
ASP.NET Core MVC application to import Crypto Exchange data from JSON files into (local) SQL Server db and display imported data (funds + order book).

# Manual Testing Checklist
- [x] App starts, migration runs, db is created.
